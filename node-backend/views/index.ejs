 <table rules="all">
    <thead>
      <tr>
        <th>machine id</th>
        <th>version</th>
        <th>service</th>
      </tr>
    </thead>
    <tbody>
      <%if (data.length >0){ %>
      <% data.forEach(function(row){ %>
      <tr>
        <td><%= row.machine_id %></td>
        <td><%= row.version %></td>
        <td><%= row.services %></td>
      </tr>
      <% })}else{ %>
        <tr>
            <td colspan="6">No Record Found</td>
            
          </tr>
        <% } %>
    </tbody>
  </table>

